# Neural Complete 游戏评估标准

本项目不只要求“机器学习算法真实运行”，还要求玩法本身接近《图灵完备》式的构造、实验与调试体验。

## 100 分游戏性评分

| 维度 | 权重 | 核心问题 |
|---|---:|---|
| 构造自由度 | 20 | 玩家是在真正“搭模型”，还是只是在调几个预设参数？ |
| 解法空间 | 15 | 是否存在多种结构上真正不同的可行解，而不是唯一正确配置？ |
| 因果可理解性 | 15 | 玩家能否看到“结构改变 → 内部表示改变 → 训练动态改变 → 输出/泛化改变”？ |
| ML 真实性 | 15 | 参数、表示或策略是否真的由数据、梯度或奖励更新，而非预制动画？ |
| 调试能力 | 10 | 玩家能否检查权重、激活、梯度、feature map、错误样本、Q-values 等内部状态？ |
| 失败质量 | 10 | 失败是否产生可诊断、可学习的现象，而不是只显示 FAIL？ |
| 教学迁移 | 10 | 通关后是否真正理解一个能迁移到其他问题的 ML 概念？ |
| 操作 / UI | 5 | 搭建、运行、观察、修改的循环是否清晰、顺畅？ |

总分：100。

## 关键维度说明

### 1. 构造自由度

这是最重要的指标。

理想状态下，玩家应能够主动决定模型结构，而不是只寻找开发者预先藏好的参数组合。例如：

- 添加 / 删除神经元或层；
- 连接不同计算节点；
- 选择激活、聚合、损失或优化组件；
- 改变数据流；
- 将已经构造的结构封装并复用；
- 组合出开发者没有预先枚举过的有效结构。

如果一关的主要交互只是 slider、select 和“训练”按钮，那么即使算法完全真实，构造自由度仍然较低。

### 2. 解法空间

同一目标应允许多种不同思路完成，而不是存在唯一显然正确的配置。

建议至少检查：

- 是否存在至少 3 类结构上不同的通关方案；
- 是否允许在参数量、训练速度、稳定性、泛化能力之间取舍；
- 是否存在“次优但可行”的方案；
- 是否能通过不同表示或结构达到相同任务目标。

可用一个粗略指标描述：

```text
Solution Diversity
= 可通关的结构类别数 × 各类别内部的有效变化范围
```

### 3. 因果可理解性

玩家修改一个组件后，应能够追踪结果为什么变化。

理想的观察链：

```text
结构改变
→ 内部表示改变
→ 训练动态改变
→ 输出改变
→ 泛化能力改变
```

因此可视化不能只是装饰，而应直接服务于调试与推理。

## 硬门槛

以下项目不计入 100 分游戏性评分，但任何正式关卡都必须满足：

- 算法真实执行；
- 参数确实由训练数据、梯度或 reward 更新；
- 错误配置真实可失败；
- train 与 hidden/test 明确分离；
- 固定 seed 时实验可复现；
- 不存在 NaN / Inf 等数值异常；
- 存档与刷新状态正确；
- 浏览器 console / page error 为 0；
- 最终评测不能通过读取答案或预制结果作弊。

这些条件只能证明“模拟器没有作弊”，不能证明“游戏好玩”。

## 北极星问题

评估任何关卡时，优先回答：

> 玩家到底是在寻找一种自己构造出来的模型，还是在寻找开发者预先藏好的正确参数组合？

只有前者才符合 Neural Complete 的长期方向。

## 当前自动测试的定位

现有 `e2e_check.py` 主要验证硬门槛与 ML 正确性，例如：

- Boundary hidden accuracy；
- XOR 非线性表示是否能通过隐藏集；
- Conv learned kernel 是否泛化；
- Autoencoder hidden MSE；
- Q-Learning 全图 success / avg steps；
- 状态持久化、响应式布局和 console error。

这些测试应该继续保留，但它们不能替代上面的游戏性评分。

后续每新增或重做一个 Demo，除了自动测试通过外，还应按本文件进行人工设计评审。
